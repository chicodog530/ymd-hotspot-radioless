{
  "schema": 1,
  "fields": [
    {
      "key": "label",
      "type": "string",
      "label": "Heartbeat label",
      "default": "YWD service plugin alive",
      "max_length": 80,
      "help": "Printed to the plugin journal with each heartbeat."
    },
    {
      "key": "interval_s",
      "type": "integer",
      "label": "Heartbeat interval (seconds)",
      "default": 60,
      "min": 10,
      "max": 300,
      "help": "Long intervals keep the Pi Zero overhead essentially negligible. Restart the service after changing this value."
    }
  ]
}
